[package]
name = "oak_idl"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
async-trait = "*"
prost = { version = "*", default-features = false, features = ["prost-derive"] }

[build-dependencies]
oak_idl_build = { path = "../oak_idl_build" }
