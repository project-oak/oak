[package]
name = "micro_rpc"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
async-trait = "*"
prost = { workspace = true }

[build-dependencies]
micro_rpc_build = { workspace = true }
