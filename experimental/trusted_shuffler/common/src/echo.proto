syntax = "proto3";
   
package experimental.trusted_shuffler.echo;

service Echo {
  rpc Echo (EchoRequest) returns (EchoResponse);
}

message EchoRequest {
  bytes value_to_echo = 1;
}

message EchoResponse {
  bytes echoed_value = 1;
}