[package]
name = "split_grpc_client"
version = "0.1.0"
authors = ["Conrad Grobler <grobler@google.com>"]
edition = "2018"

[dependencies]
env_logger = "0.7"
log = "0.4"
prost = "0.6"
tonic = "0.1"
tokio = { version = "0.2", features = ["macros"] }

[build-dependencies]
tonic-build = "0.1"
