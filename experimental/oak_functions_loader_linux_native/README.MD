# Oak Functions Loader Linux Native

A thin wrapper for the "baremetal" runtime to compile it as a linux binary. The wrapping loader provides communication with a launcher by connecting to a file desciptor.
