# This crate has its own workspace as it must be built independently.
[workspace]

[package]
name = "bare_metal_runtime"
version = "0.1.0"
authors = ["Conrad Grobler <grobler@google.com>"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
vga = "*"
bootloader = "0.9.8"
