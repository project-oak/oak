[package]
name = "hello_world_grpc"
version = "0.1.0"
authors = ["David Drysdale <drysdale@google.com>"]
edition = "2018"
license = "Apache-2.0"

[dependencies]
prost = "*"
tonic = { version = "*", features = ["tls"] }

[build-dependencies]
oak_utils = "*"
