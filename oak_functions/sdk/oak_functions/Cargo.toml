[package]
name = "oak_functions"
version = "0.1.0"
authors = ["Razieh Behjati <razieh@google.com>"]
edition = "2018"
license = "Apache-2.0"

[features]
logger = ["log"]
default = []

[dependencies]
log = { version = "*", optional = true }
oak_functions_abi = { path = "../../abi" }
