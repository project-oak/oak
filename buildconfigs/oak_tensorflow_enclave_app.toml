# Unlike other build configs, this one needs to run in the "default" shell instead of just the
# "rust" one, because bazel is also used by the build.rs script.
command = [
  "nix",
  "develop",
  ".#default",
  "--command",
  "timeout",
  "10",
  "touch",
  "hello.txt",
]
artifact_path = "./hello.txt"
