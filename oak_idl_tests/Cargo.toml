[package]
name = "oak_idl_tests"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]

[dev-dependencies]
async-trait = "*"
maplit = "*"
oak_idl = { path = "../oak_idl" }
prost = "*"
tokio = { version = "*", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
oak_idl_build = { path = "../oak_idl_build" }
